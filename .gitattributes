# Shell scripts and Node hooks MUST stay LF — CRLF makes bash fail with
# "$'\r': command not found" the moment this repo is cloned on macOS/Linux.
*.sh    text eol=lf
*.js    text eol=lf
*.json  text eol=lf
*.md    text eol=lf

# PowerShell stays CRLF for Windows.
*.ps1   text eol=crlf
